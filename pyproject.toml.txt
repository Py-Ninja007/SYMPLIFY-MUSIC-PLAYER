[tool.poetry]
name = "SYMPLIFY(v1.0)"
version = "0.1.0"
description = "SYMPLIFY Music Streaming"
authors = ["Pyninja"]

[tool.poetry.dependencies]
python = "^3.8"
# Add other dependencies here, e.g., requests = "^2.25.1"

[tool.poetry.dev-dependencies]
# Add development dependencies here, e.g., pytest = "^6.2.4"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
